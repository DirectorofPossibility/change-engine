# System Audit — 2026-03-07

Backup tag: `v2026.03.07-stable` at commit `b352664` on `master`

---

## CRITICAL

- [ ] Replace 35 `<img>` tags with `next/image` (DetailWayfinder, OfficialCard, FeaturedPromo, D2Footer, GuidePage, ContentCard, PageHero)
- [ ] Add `loading.tsx` skeletons for top 10 dynamic routes: content/[id], services/[id], officials/[id], policies/[id], events/[id], organizations/[id], guides/[slug], opportunities/[id], agencies/[id], benefits/[id]
- [ ] Add `error.tsx` at exchange layout level with branded error UI and retry button
- [ ] Split WayfinderSidebar mega-component (370 lines, 10 useState, 2 useEffect) into smaller server+client pieces

## HIGH — Backend

- [ ] `getWayfinderContext` — 20+ conditional branches, 10+ sequential queries per detail page; refactor into parallel batches
- [ ] `classify-pending` cron — sequential loop through 9 tables; switch to `Promise.all`
- [ ] Dashboard queries — in-memory counting without DB aggregation (`src/lib/data/dashboard.ts`)
- [ ] Duplicate classifier code between Node.js (`src/lib/classification/`) and Deno (`supabase/functions/_shared/classifier.ts`); keep one source of truth
- [ ] No pagination on `getOrganizations`, `getOfficials`, `getLearningPaths` (`src/lib/data/exchange.ts`)
- [ ] Edge function timeout risk — inline classification can exceed 60s in sync functions

## HIGH — Frontend

- [ ] Wrap card components in `React.memo`: ContentCard, ServiceCard, PolicyCard, OfficialCard, LibraryCard, OpportunityCard
- [ ] 333 inline `style={{}}` objects with runtime color math — migrate to CSS variables or Tailwind config
- [ ] 0 `error.tsx` boundaries in nested exchange routes (content, officials, policies, services)
- [ ] InfoBubble: 4 separate useEffect hooks per instance (400+ listeners on pages with many tooltips) — consolidate to 1-2
- [ ] Hydration mismatches: 20 client components access `window`/`document` without guards (WayfinderSidebar, LanguageContext, NeighborhoodBanner, ImageLightbox)

## MEDIUM — Backend

- [ ] Missing request body validation on API routes (`src/app/api/`)
- [ ] Inconsistent timeout patterns across external API calls (sync functions)
- [ ] `SELECT *` on queries that only need a few columns (various data files)

## MEDIUM — Frontend

- [ ] Lucide icons: 49 files importing individually (~3KB each); create icon barrel export or registry
- [ ] TranslatedContentGrid: client-side translation fetch could be server-side
- [ ] OfficialCard / ServiceCard / PolicyCard are near-duplicates (~200 lines redundant); consolidate into EntityCard
- [ ] Code-split DetailWayfinder (540 lines) and WayfinderCircles with React.lazy + Suspense
- [ ] ContentShelf: resize + scroll event listeners without debounce; use ResizeObserver + requestAnimationFrame
- [ ] Translation system: no request deduplication in LanguageContext; duplicate fetches for same inboxIds
- [ ] Missing accessibility: empty alt text on logos/photos, interactive SVGs lack aria-labels, no focus rings on InfoBubble buttons, missing form labels on ZIP input
- [ ] ShelfBraid content grouping logic not memoized (re-runs on every render for 100+ items)
- [ ] No virtual scrolling for long lists (TranslatedContentGrid renders all items at once)
- [ ] Missing dynamic OG metadata on some detail pages

## LOW

- [ ] Scrollbar-hide CSS defined globally but many components use inline equivalents
- [ ] `.color-bar-left` pseudo-element pattern defined in globals.css but rarely used
- [ ] Long className strings — adopt `clsx` or `classnames` for readability
- [ ] Leaflet z-index overrides using `!important` in globals.css (brittle)
